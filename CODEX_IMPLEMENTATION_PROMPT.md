# 请实现 FitnessPlan 已确认的 UI 与减脂策略

你是负责本仓库的资深 Flutter 工程师。请直接在现有 FitnessPlan 项目内完成下述功能、数据迁移、测试和实际界面验证，不要只给计划或另写一个静态演示应用。使用现有 Flutter + Riverpod + go_router + Drift/SharedPreferences 架构，保持离线、本地数据优先。

## 0. 必须先阅读并查看的材料

- 仓库中适用的 AGENTS.md 与现有开发说明。
- `design-handoff/README.md`。
- `design-handoff/fat-loss-strategy.md`，完整阅读公式、参数限制、数据历史与证据说明。
- 用图片查看工具实际打开以下四张图，而不是只读取文件名：
  - `design-handoff/images/01-product-overview.png`
  - `design-handoff/images/02-four-themes.png`
  - `design-handoff/images/03-water-and-plain-plus.png`
  - `design-handoff/images/04-diet-strategy-flow.png`

最终优先级：本提示词的文字要求 > 算法文档的计算规则 > 水杯与纯加号细节图 > 策略流程图 > 四主题图 > 产品全景图。较早图中的圆底加号和厚重杯盖已被用户否决。图中示例数字不能硬编码，生成图的字形、数值及误差不能照搬。

## 1. 现有工作保护与范围

先运行 git status/diff，理解并保留现有未提交修改。交接时已存在步数传感器、步数同步、provider、首页、本地化和 pubspec 等修改；当前状态可能已变化，以现场为准。不要 reset、覆盖、回滚他人改动，不删除已有功能或个人数据。

用户现在授权本提示词的执行者实现代码、增加必要文件和进行测试。无需重新询问是否开始或是否采用已确认的设计。遇到常规工程选择自行判断；权限和无法解决的数据歧义据实说明。不要自动提交、推送、发布或安装到用户真机。

## 2. 视觉系统与核心页面

四套主题统一组件、布局及语义：

- 清新绿（新用户默认）：#F6F8F5 浅底、白卡、#246B50 主色、深色文字。
- 极光：深海蓝底、青绿强调与局部光感，正文保持高对比。
- 暖阳：奶油底、暖橙主色、深棕文字。
- 石墨：哑光深灰、银色强调，按钮文字具有足够对比。

建立或重构可复用颜色、间距、字号、圆角、分隔线、图标及状态 token；不要四份页面复制粘贴。保持营养素语义颜色跨主题一致。原有主题 ID 做兼容映射或迁移，保留用户已选主题，只有没有选择的新用户采用默认主题。

统一优化今日、食材库、饮食录入、身体记录、我的、主题外观页面。使用原有能力与数据源，支持中英文，不凭概念图凭空添加无后端功能。食材搜索、分类、收藏、最近记录、食物详情、克数与餐次录入需可用；身体记录保留体脂、训练、便签及历史能力；我的页保留原有工具、提醒等入口。

## 3. 首页不可变需求

1. 日期导航保留历史日期选择和补录能力。
2. 今日热量只有一张整合主卡：上排左侧剩余热量、中间圆环、最右侧小水杯。杯身加杯盖总高度与圆环直径相近，不能把水杯放到下一行单独占一块区域。
3. 同卡下排严格为蛋白质、碳水、脂肪、饮水四列；显示当前值/目标和进度。宽度不足优先分行显示数字与单位，不能溢出或把饮水移到独立卡。
4. 水杯参考细节图：轻薄描边、圆润底部、半透明水色、柔和波纹、独立薄杯盖。用 Flutter CustomPainter/现有原生绘制组件实现，可维护且随主题变化；不要用整张生成图、厚重3D瓶子或静态截图替代。
5. 保留点击杯身加水、点击杯盖撤回的现有语义，检查现有 repository 的实际实现后接入。默认每次250ml，原有容量/目标设置可继续使用；水位动画、轻反馈可增强，但系统减少动画设置应受尊重。0水量不可减为负数；超过目标显示真实数值，水位绘制可封顶；历史日期操作规则沿用现有约束。
6. 今日训练、今日饮食默认折叠，展开状态相互独立；初次进入和切换日期默认收起。点击标题区域或箭头展开/收起。
7. 两个区块各自有一个**纯加号**，没有圆形背景、边框或阴影。加号与展开箭头分开，点击加号不能触发展开；分别调用原有添加训练与添加饮食流程。保留现有菜单操作（例如复制昨日、预设等），不要因图中未画而删除。
8. 纯图标仍要有足够触控面积与语义标签。不要用极小命中区域换取图标简洁。
9. 不新增重复的大型“记录饮食”按钮或中间悬浮加号。
10. 保持原四图标悬浮胶囊导航：今日、食材、记录、我的；**不显示文字标签**，保留可访问性标签、选中态与页面状态。
11. 步数同步、历史饮食、训练与提醒等已有功能继续可用；界面重排不能静默移除功能。

## 4. 减脂策略与算法

实现完整的策略选择、参数编辑、计划预览、生效、持久化、日期查询、复核与暂停/切换链路。

主策略为均衡缺口（默认）、碳循环、碳水渐降；不要擅自添加极端低热量、自动禁食、生酮或宣传性“加速燃脂”算法。进食时段如做仅为提醒，不改变配额，非本次必需新增项。

遵循 `design-handoff/fat-loss-strategy.md` 的全部具体计算规则：

- TDEE估算、参考体重、P/F基线、碳水残余、缺口设置范围及可行性限制。
- 均衡策略每日同目标。
- 碳循环：七日高/中/低得分去均值，统一缩放振幅满足每日上下界，同时保持七日总预算；P/F稳定，C承担日间差异。不能各天独立 clamp 破坏总和。
- 渐降：至少14天观察、必要时21天、体重与完整饮食门槛、7日均值比较、每次25g碳水/100kcal候选调整、用户确认与下限约束。不得到期自动减量。
- 输入不合法、宏量不可行、摄入边界、训练供能等状态必须显式处理。文档定义的产品限制不是临床认证，不得宣称已被医学验证。
- 切换到维持/增肌时停用减脂策略，不把渐降扣减继续叠加；保留原目标模式行为。

原算法有 calorieAdjustment、平台期逻辑和固定 profile.targets，先理解并统一入口。不要旧平台期扣减一次、新渐降再扣一次；不要把计划缺口重复减入目标；不要重复加回活动系数已涵盖的运动热量。

建立纯函数计算层，避免在 widget 内散落公式。内部保留精度，展示舍入与计算分离。检查 NaN、无穷、负值、零值、非法周期和异常日期。

## 5. 每日目标、迁移与历史

引入可版本化策略计划与按本地日期确定的每日目标（名称按代码风格决定）：保存生效日期、参考体重、TDEE、策略参数、日型、能量/宏量、生成原因和必要覆盖。

首页、餐食录入、日历缺口、历史记录、趋势摘要均读取同一个按日期查询的目标来源。不要只改首页数字。

历史使用当时有效的计划/目标，不因今天更换策略或更新体重而改变。缺少旧快照的记录明确区分为旧版估算；不得伪造精确历史。已有用户默认保留原目标，显式选择策略后才变更。

新策略默认下个完整周期生效，允许用户预览后选择今天开始；中途周期明确显示覆盖日期与预算。训练排期变更不偷偷重算已过去的日期，不把超吃转为明日惩罚性扣减。未来日期仅展示计划预览，不虚构实际摄入。

持久化迁移必须向后兼容、事务性、可测试，不能重建或清空用户库。如果现有数据不含“完整饮食记录”标志，新增按日确认能力并让历史默认为未知，不能以有一餐记录等价全天完整。

## 6. 新增 UI

- 我的 → 营养目标 → 减脂策略；首页主卡策略标签提供快捷入口。
- 策略选择：三张选择项、简明说明、进入配置。
- 碳循环计划：日均目标、七日高/中/低安排、选中日热量/宏量、每周预算、生效日期、应用前预览；训练匹配是需确认的建议。
- 渐降：当前阶段、热量/碳水、复核日期、完整度、保持或调整建议、确认新阶段。显示观察不足状态。
- 首页主卡策略标签例如“碳循环 · 高碳日”；今日目标、剩余、圆环与四指标同步；周均作为次级信息。
- 计算依据与适用范围放入策略详情，首页不堆砌公式。图上研究说明不要求逐字照搬，但不得作虚假效果宣传。

## 7. 数值验收例

合成测试输入：参考体重75kg、TDEE2400kcal、确认平均目标2000kcal，P150g/F60g。不是把这个例子硬编码为所有人的默认值。

- 七日高/中/高/中/中/低/低：2200/2000/2200/2000/2000/1800/1800kcal。
- 碳水分别265/215/265/215/215/165/165g。
- 周总14000kcal，逐日满足4P+4C+9F=E；任意其他合法排期也保持预算与边界。
- 高碳日已摄入1120 → 剩余1080、摄入圆环约51%；中碳日剩余880；低碳日剩余680。
- 水量1250/2000ml不受高低碳热量倍率影响。
- 渐降候选2000/215g →1900/190g →1800/165g；每步均经过复核确认，此例到下限后不得继续降低。

## 8. 验证与完成要求

先查看项目现有测试和构建方式，执行适合变更的 format、flutter analyze、flutter test；修改本地化/Drift时运行项目对应生成流程并检查差异。不能因为工具不可用就声称通过，需记录明确阻碍。

重点测试：七日任意日型预算守恒、边界统一收缩、宏量能量一致、无效输入、日期/时区/DST、计划版本生效、历史不被重算、迁移保留数据、缺失记录不触发渐降、阶段下限、未成年等不适用流程、超额不扣明日配额，以及原步数/饮食/训练能力的回归。

Widget/交互验证：四主题、首页四列不溢出、纯加号命中不展开、两区块独立折叠、原添加流程、水杯加减与0值、主题重启后恢复、历史/未来日期正确。至少检查360逻辑像素窄屏与较大文字，图片不能作为自动化通过的证据。

使用项目可用运行环境生成真实运行截图，放入 `design-handoff/implementation-preview/`；与参考图对照修正间距、对齐、层级和水杯外观，不把概念图冒充运行截图。如果确实无法运行，写明限制并完成其他可执行验证。

最终报告实现了什么、主要文件、迁移行为、通过的测试、实际截图路径及尚存限制。不要把待专业审阅参数描述为已认证，也不要把未运行的测试描述为已通过。不要以仅完成模型层或几个页面为完成，贯通UI、算法、存储和历史后再交付。
